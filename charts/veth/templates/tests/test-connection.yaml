apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "veth.fullname" . }}-test-connection"
  labels:
    {{- include "veth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "veth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
