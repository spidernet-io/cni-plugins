apiVersion: v1
kind: Service
metadata:
  name: {{ include "veth.fullname" . }}
  labels:
    {{- include "veth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "veth.selectorLabels" . | nindent 4 }}
